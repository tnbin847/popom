#
# 로컬 개발 프로파일(Profile) 파일
#

## 스프링 설정
spring:
  config:
    activate:
      on-profile:
        - dev
  # 로컬 데이터베이스 연결 정보 등록
  datasource:
    hikari:
      jdbc-url: jdbc:mariadb://localhost:3306/popom_db?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
      username: tnbin847
      password: 1315
      driver-class-name: org.mariadb.jdbc.Driver

## 로깅 레벨 설정
logging:
  level:
    root: info
    web: debug